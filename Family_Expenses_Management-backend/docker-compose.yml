version: "3.9"
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MONGODB_URI=mongodb://mongo:27017
      - MONGODB_DB_NAME=family_expense_manager
      - SECRET_KEY=secret_key_here
      - ACCESS_TOKEN_EXPIRE_MINUTES=1440
      - EMAIL_USER=doducvinh2k3@gmail.com
      - EMAIL_PASSWORD=ejei ozyx pxsn ajdg
      - EMAIL_SERVER=smtp.gmail.com
      - EMAIL_PORT=587
    depends_on:
      - mongo

  mongo:
    image: mongo:6
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
